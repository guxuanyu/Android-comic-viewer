<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ganger.comic">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/mypicc"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/NoBar">
        <activity
            android:name=".MainActivity"
            android:theme="@style/NoBar"></activity>
        <activity android:name=".ImageBrowseActivity" />
        <activity android:name=".ImageBrowseActivity2" />
        <activity
            android:name=".WelcomeActivity"
            android:theme="@style/NoBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainMainActivity"></activity>
    </application>

</manifest>